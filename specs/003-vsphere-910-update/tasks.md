# Tasks: vSphere 9.1.0.0 API Binding Update

**Input**: Design documents from `/specs/003-vsphere-910-update/`  
**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/README.md](./contracts/README.md), [quickstart.md](./quickstart.md)

**Tests**: Not requested as new test authoring — verification via `cargo build`, `cargo test`, generator idempotency, and quickstart checklist (SC-001–SC-011).

**Organization**: Tasks grouped by user story (P1 → P2) with shared foundation first.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no blocking deps on incomplete tasks)
- **[Story]**: US1, US2, US3, US4 per [spec.md](./spec.md)

## Path Conventions

- Generator: `vim_build/`
- Library: `vim_rs/`, `vim_macros/`
- MCP: `mcp/`
- Examples workspace: `examples/`
- Root docs: `CHANGELOG.md`

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Confirm branch, design artifacts, and release contract before touching code.

- [x] T001 Confirm feature branch `003-vsphere-910-update` is checked out
- [x] T002 Review release contract in `specs/003-vsphere-910-update/contracts/README.md` and validation runbook in `specs/003-vsphere-910-update/quickstart.md`
- [x] T003 Confirm canonical input file exists at `vim_build/data/vi_json_openapi_specification_v9_1_0_0.json`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Pre-flight OpenAPI validation and wire all hard-coded spec paths to 9.1.0.0. **Blocks all user stories.**

**⚠️ CRITICAL**: No generator run until this phase completes.

- [x] T004 Validate `DatastoreAccessible_enum` in `vim_build/data/vi_json_openapi_specification_v9_1_0_0.json` uses string values `"True"` and `"False"` (not JSON booleans); if wrong, fix `vim_build/data/vi-json.yaml` and re-convert before proceeding (FR-013)
- [x] T005 Run `cargo test -p openapi30` to confirm the OpenAPI file parses (update fixture path in T009 first if tests fail on missing file)
- [x] T006 [P] Update generator input path to `vi_json_openapi_specification_v9_1_0_0.json` in `vim_build/src/main.rs`
- [x] T007 [P] Update OpenAPI input path to `vi_json_openapi_specification_v9_1_0_0.json` in `mcp/data_processing/data_transformer/src/main.rs`
- [x] T008 [P] Update staleness-check path to `vi_json_openapi_specification_v9_1_0_0.json` in `mcp/server/build.rs`
- [x] T009 [P] Update embedded test fixture path to `vi_json_openapi_specification_v9_1_0_0.json` in `openapi30/src/openapi.rs`
- [x] T010 [P] Update unit-test fixture path to `vi_json_openapi_specification_v9_1_0_0.json` in `vim_build/src/vim_model/loader.rs`
- [x] T011 Re-run `cargo test -p openapi30` after path updates in `openapi30/src/openapi.rs`

**Checkpoint**: All five path constants point at 9.1.0.0 JSON; spec pre-flight passes.

---

## Phase 3: User Story 1 — Access vSphere 9.1.0.0 API surface (Priority: P1) 🎯 MVP

**Goal**: Regenerate all `vim_rs` bindings from the 9.1.0.0 OpenAPI definition with no hand-edits.

**Independent Test**: `cd vim_build && cargo run --bin generate --release` completes; `vim_rs/src/types/` and `vim_rs/src/mo/` reflect 9.1.0.0; second generator run produces zero diff (SC-005).

### Implementation for User Story 1

- [x] T012 [US1] Run `cargo run --bin generate --release` from `vim_build/` to regenerate `vim_rs/src/types/*` and `vim_rs/src/mo/*`
- [x] T013 [US1] Verify generator also emitted `vim_macros/src/field_data.rs` and `mcp/server/src/field_data.rs` in the same run (no hand-edits)
- [x] T014 [US1] Re-run `cargo run --bin generate --release` from `vim_build/` and confirm `git diff` is empty on `vim_rs/src/`, `vim_macros/src/field_data.rs`, and `mcp/server/src/field_data.rs` (SC-005)
- [x] T015 [US1] Run `cargo check -p vim_rs` to confirm generated bindings compile before full test matrix

**Checkpoint**: 9.1.0.0 bindings generated; idempotent; initial compile check passes.

---

## Phase 4: User Story 3 — Macro and MCP tooling stay in sync (Priority: P2)

**Goal**: Field-path metadata and MCP API database match the same 9.1.0.0 definition as core bindings.

**Independent Test**: `cargo run -p data-transformer --release --features cuda` succeeds; `cargo build --release -p vim_mcp_server` succeeds (SC-007).

**Depends on**: Phase 3 (field_data regenerated by same generator run).

### Implementation for User Story 3

- [x] T016 [US3] Confirm `vim_macros/src/field_data.rs` and `mcp/server/src/field_data.rs` were produced by the US1 generator run (not edited manually)
- [x] T017 [US3] Run `cargo run -p data-transformer --release --features cuda` from `mcp/` to rebuild `mcp/data/api_database.bin`
- [x] T018 [US3] Run `cargo build --release -p vim_mcp_server` from `mcp/` and confirm `mcp/server/build.rs` staleness check passes
- [x] T019 [US3] Run `cargo test -p vim_macros` to verify macro field-path resolution against regenerated `vim_macros/src/field_data.rs`

**Checkpoint**: MCP database and server build against 9.1.0.0; macros resolve paths.

---

## Phase 5: User Story 4 — Coordinated 0.6.0 minor release (Priority: P2)

**Goal**: Align crate versions at **0.6.0** (breaking minor — not 0.5.1 patch), retitle CHANGELOG, and purge all 0.5.1 release-target references (FR-006, FR-007, FR-011, FR-015).

**Independent Test**: `vim_rs/Cargo.toml` and `vim_macros/Cargo.toml` report `0.6.0`; `CHANGELOG.md` contains `[0.6.0]` entry classifying a breaking minor release; `rg '0\.5\.1'` finds no release-target hits in manifests/docs (SC-003, SC-006, SC-011).

**Note**: T020–T024 were initially completed at 0.5.1; semver clarification (2026-06-26) requires correction to **0.6.0**.

### Implementation for User Story 4

- [x] T020 [P] [US4] Bump `version` to `0.6.0` and sync `vim_macros` path dep to `0.6.0` in `vim_rs/Cargo.toml`
- [x] T021 [P] [US4] Bump `version` to `0.6.0` in `vim_macros/Cargo.toml`
- [x] T022 [P] [US4] Update `vim_rs` path dependency version to `0.6.0` in `examples/snippets/Cargo.toml`
- [x] T023 [P] [US4] Update `vim_rs` path dependency version to `0.6.0` in `examples/vtui/Cargo.toml`
- [x] T024 [US4] Retitle `CHANGELOG.md` section from `[0.5.1]` to `[0.6.0]`; add breaking-minor classification and semver rationale (R9); list breaking changes including `EventFilterSpec` field additions (FR-011)
- [x] T037 [P] [US4] Update `vim_rs` version mention to **0.6.0** in `mcp/README.md`
- [x] T038 [US4] Run semver audit per `specs/003-vsphere-910-update/quickstart.md` Step 7 — confirm zero `0.5.1` release-target hits in manifests and release docs (SC-011)

**Checkpoint**: Versions aligned at 0.6.0; CHANGELOG reflects breaking minor release; no 0.5.1 release targets remain.

---

## Phase 6: User Story 2 — Entire monorepo compiles cleanly (Priority: P1)

**Goal**: Every crate and sample project builds without errors after the spec upgrade and version bump.

**Independent Test**: Full matrix in `specs/003-vsphere-910-update/quickstart.md` Step 3 passes with zero errors (SC-001, SC-002, SC-004).

**Depends on**: Phases 3, 4, 5 (generated bindings, MCP rebuild, version bump).

### Implementation for User Story 2

- [x] T025 [US2] Run `cargo build` and `cargo test` in `vim_rs/` (default features)
- [x] T026 [US2] Run `cargo build --all-features` and `cargo test --all-features` in `vim_rs/`
- [x] T027 [P] [US2] Run `cargo test` in `vim_build/` and `openapi30/`
- [x] T028 [US2] Run `cargo build --bins` in `examples/` (all sample binaries, default features)
- [x] T029 [US2] Run `cargo build --bins --all-features` in `examples/`
- [x] T030 [US2] Fix compile errors in hand-written sample or test sources only (e.g. under `examples/snippets/src/`, `examples/vtui/src/`, `vim_rs/tests/`) for spec-driven API renames or removals — **do not** patch generated files (FR-010)
- [x] T031 [US2] Document breaking API symbols in the `CHANGELOG.md` `[0.6.0]` entry (finalize after T024 retitle; include `EventFilterSpec.auditable`, `EventFilterSpec.audit_id`)
- [x] T039 [US2] Re-run `cargo test -p vim_rs` and `cargo build --bins` in `examples/` after Phase 5 semver correction (T020–T024)

**Checkpoint**: Full monorepo compile and test matrix green at **0.6.0**.

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Release gates, metrics, and final validation.

- [x] T032 Capture `vim_rs` debug and release build times and `examples/vtui` release binary size; compare against 0.5.0 baseline and note in `CHANGELOG.md` (SC-008, Principle III)
- [x] T033 [P] Update `mcp/data_processing/data_transformer/README.md` if it still references the 9.0.x OpenAPI filename
- [x] T034 Run full validation checklist from `specs/003-vsphere-910-update/quickstart.md` (all steps, including Step 7 semver audit)
- [x] T035 If any failure occurred during implementation, document root-cause classification (spec vs generator vs downstream) per FR-013/SC-009 in `CHANGELOG.md` or PR notes
- [x] T036 Audit and update stale 9.0.x **binding-target** references in hand-written sources per FR-014 (`README.md`, `CLAUDE.md`, `vim_rs/src/core/client.rs`, `mcp/README.md`, and related MCP/LLM docs); confirm via repo search excluding generated `vim_rs/src/` types/mo and archived `vim_build/data/vi_json_openapi_specification_v9_0_0_0_24798170.json`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — start immediately
- **Foundational (Phase 2)**: Depends on Setup — **BLOCKS all user stories**
- **US1 (Phase 3)**: Depends on Foundational — **BLOCKS US2, US3**
- **US3 (Phase 4)**: Depends on US1 (field_data from generator)
- **US4 (Phase 5)**: Depends on US1; **0.6.0 semver correction** before T039/T034 final validation
- **US2 (Phase 6)**: Depends on US1 + US3; T039 depends on Phase 5 completion
- **Polish (Phase 7)**: T034 depends on Phase 5 + T039

### User Story Dependencies

```text
Foundational → US1 → US3 ─┐
              └→ US4 (0.6.0) ─┼→ US2 (T039) → Polish (T034)
```

- **US1 (P1)**: MVP — regenerated 9.1.0.0 bindings; independently verified via idempotency check
- **US2 (P1)**: Full compile matrix; T025–T030 complete; T031/T039 pending 0.6.0 semver correction
- **US3 (P2)**: MCP pipeline; complete
- **US4 (P2)**: Version + CHANGELOG at **0.6.0**; T020–T024, T037–T038 pending

### Parallel Opportunities

- **Phase 2**: T006–T010 (five path updates) can run in parallel after T004
- **Phase 5**: T020–T023 and T037 can run in parallel
- **Phase 6**: T027 can run in parallel with T025–T026 once `vim_rs` builds

### Parallel Example: Version bump to 0.6.0 (US4)

```bash
Task T020: vim_rs/Cargo.toml
Task T021: vim_macros/Cargo.toml
Task T022: examples/snippets/Cargo.toml
Task T023: examples/vtui/Cargo.toml
Task T037: mcp/README.md
# Then T024: CHANGELOG.md retitle + breaking-minor notes
# Then T038: semver audit (SC-011)
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup  
2. Complete Phase 2: Foundational (spec validation + path wiring)  
3. Complete Phase 3: US1 (generator run + idempotency)  
4. **STOP and VALIDATE**: `cargo check -p vim_rs` passes; regeneration is idempotent  

### Incremental Delivery

1. Setup + Foundational → spec ready ✅  
2. US1 → 9.1.0.0 bindings exist (MVP) ✅  
3. US3 → MCP database and server aligned ✅  
4. US4 → versions at **0.6.0** ✅  
5. US2 → full monorepo green; re-validate after US4 (T039)  
6. Polish → final quickstart checklist (T034)  

### Failure Triage (FR-013)

On any generator, compile, or test failure:

1. **Spec first** — re-check `vim_build/data/vi_json_openapi_specification_v9_1_0_0.json` and YAML conversion (boolean vs string enums)  
2. **Generator** — only after spec confirmed  
3. **Downstream** — hand-written sample/test fixes only  
4. **Never** patch generated output in `vim_rs/src/`, `vim_macros/src/field_data.rs`, or `mcp/server/src/field_data.rs`

---

## Notes

- Total tasks: **39** (T001–T039); T037–T039 added for 0.6.0 semver correction (FR-015, SC-011)
- **0.5.1 MUST NOT** be published — retitle CHANGELOG and purge manifest references (spec clarification 2026-06-26)
- Generated files MUST NOT be hand-edited (Principle I)
- Prior 9.0.x JSON at `vim_build/data/vi_json_openapi_specification_v9_0_0_0_24798170.json` may remain for reference
- Live vCenter testing is recommended post-release but not a task gate (spec assumption)
- Suggested next step: commit and publish **0.6.0** (optional: `/speckit-git-commit`)
