# Tasks: vSphere 9.1.0.0 API Binding Update

**Input**: Design documents from `/specs/003-vsphere-910-update/`  
**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/README.md](./contracts/README.md), [quickstart.md](./quickstart.md)

**Tests**: Not requested as new test authoring — verification via `cargo build`, `cargo test`, `cargo outdated`, generator idempotency, and quickstart checklist (SC-001–SC-014).

**Organization**: Tasks grouped by user story (P1 → P2) with shared foundation first. **Phases 1–7 complete** (spec bump + 0.6.0). **Phase 8 (US5)** is the remaining dependency-refresh work.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no blocking deps on incomplete tasks)
- **[Story]**: US1–US5 per [spec.md](./spec.md)

## Path Conventions

- Generator: `vim_build/`
- Library: `vim_rs/`, `vim_macros/`
- MCP: `mcp/`
- Examples workspace: `examples/`
- Root docs: `CHANGELOG.md`

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Confirm branch, design artifacts, and release contract before touching code.

- [x] T001 Confirm feature branch `003-vsphere-910-update` is checked out
- [x] T002 Review release contract in `specs/003-vsphere-910-update/contracts/README.md` and validation runbook in `specs/003-vsphere-910-update/quickstart.md`
- [x] T003 Confirm canonical input file exists at `vim_build/data/vi_json_openapi_specification_v9_1_0_0.json`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Pre-flight OpenAPI validation and wire all hard-coded spec paths to 9.1.0.0. **Blocks all user stories.**

**⚠️ CRITICAL**: No generator run until this phase completes.

- [x] T004 Validate `DatastoreAccessible_enum` in `vim_build/data/vi_json_openapi_specification_v9_1_0_0.json` uses string values `"True"` and `"False"` (not JSON booleans); if wrong, fix `vim_build/data/vi-json.yaml` and re-convert before proceeding (FR-013)
- [x] T005 Run `cargo test -p openapi30` to confirm the OpenAPI file parses (update fixture path in T009 first if tests fail on missing file)
- [x] T006 [P] Update generator input path to `vi_json_openapi_specification_v9_1_0_0.json` in `vim_build/src/main.rs`
- [x] T007 [P] Update OpenAPI input path to `vi_json_openapi_specification_v9_1_0_0.json` in `mcp/data_processing/data_transformer/src/main.rs`
- [x] T008 [P] Update staleness-check path to `vi_json_openapi_specification_v9_1_0_0.json` in `mcp/server/build.rs`
- [x] T009 [P] Update embedded test fixture path to `vi_json_openapi_specification_v9_1_0_0.json` in `openapi30/src/openapi.rs`
- [x] T010 [P] Update unit-test fixture path to `vi_json_openapi_specification_v9_1_0_0.json` in `vim_build/src/vim_model/loader.rs`
- [x] T011 Re-run `cargo test -p openapi30` after path updates in `openapi30/src/openapi.rs`

**Checkpoint**: All five path constants point at 9.1.0.0 JSON; spec pre-flight passes.

---

## Phase 3: User Story 1 — Access vSphere 9.1.0.0 API surface (Priority: P1) 🎯 MVP

**Goal**: Regenerate all `vim_rs` bindings from the 9.1.0.0 OpenAPI definition with no hand-edits.

**Independent Test**: `cd vim_build && cargo run --bin generate --release` completes; `vim_rs/src/types/` and `vim_rs/src/mo/` reflect 9.1.0.0; second generator run produces zero diff (SC-005).

### Implementation for User Story 1

- [x] T012 [US1] Run `cargo run --bin generate --release` from `vim_build/` to regenerate `vim_rs/src/types/*` and `vim_rs/src/mo/*`
- [x] T013 [US1] Verify generator also emitted `vim_macros/src/field_data.rs` and `mcp/server/src/field_data.rs` in the same run (no hand-edits)
- [x] T014 [US1] Re-run `cargo run --bin generate --release` from `vim_build/` and confirm `git diff` is empty on `vim_rs/src/`, `vim_macros/src/field_data.rs`, and `mcp/server/src/field_data.rs` (SC-005)
- [x] T015 [US1] Run `cargo check -p vim_rs` to confirm generated bindings compile before full test matrix

**Checkpoint**: 9.1.0.0 bindings generated; idempotent; initial compile check passes.

---

## Phase 4: User Story 3 — Macro and MCP tooling stay in sync (Priority: P2)

**Goal**: Field-path metadata and MCP API database match the same 9.1.0.0 definition as core bindings.

**Independent Test**: Local data-transformer run succeeds; `cargo build --release -p vim_mcp_server` succeeds after local `mcp/data/api_database.bin` exists (SC-007).

**Depends on**: Phase 3 (field_data regenerated by same generator run). **Re-run T056–T057 after US5 `bincode` 3 migration.**

### Implementation for User Story 3

- [x] T016 [US3] Confirm `vim_macros/src/field_data.rs` and `mcp/server/src/field_data.rs` were produced by the US1 generator run (not edited manually)
- [x] T017 [US3] Run `cargo run -p data-transformer --release --features cuda` from `mcp/` to rebuild gitignored `mcp/data/api_database.bin`
- [x] T018 [US3] Run `cargo build --release -p vim_mcp_server` from `mcp/` and confirm `mcp/server/build.rs` staleness check passes
- [x] T019 [US3] Run `cargo test -p vim_macros` to verify macro field-path resolution against regenerated `vim_macros/src/field_data.rs`
- [ ] T056 [US3] Re-run `cargo run -p data-transformer --release --features cuda` from `mcp/` after `bincode` 3 migration (T050) to regenerate local `mcp/data/api_database.bin` — do not commit `mcp/data/` (FR-005, R12)
- [ ] T057 [US3] Re-run `cargo build --release -p vim_mcp_server` from `mcp/` after T056 and confirm SC-007

**Checkpoint**: MCP database and server build against 9.1.0.0 with current serialization format.

---

## Phase 5: User Story 4 — Coordinated 0.6.0 minor release (Priority: P2)

**Goal**: Align crate versions at **0.6.0**, CHANGELOG complete, purge 0.5.1 release-target references (FR-006, FR-007, FR-011, FR-015).

**Independent Test**: `vim_rs/Cargo.toml` and `vim_macros/Cargo.toml` report `0.6.0`; `CHANGELOG.md` contains `[0.6.0]` with breaking-minor classification **and** enumerated major dependency bumps; `rg '0\.5\.1'` finds no release-target hits (SC-003, SC-006, SC-011).

### Implementation for User Story 4

- [x] T020 [P] [US4] Bump `version` to `0.6.0` and sync `vim_macros` path dep to `0.6.0` in `vim_rs/Cargo.toml`
- [x] T021 [P] [US4] Bump `version` to `0.6.0` in `vim_macros/Cargo.toml`
- [x] T022 [P] [US4] Update `vim_rs` path dependency version to `0.6.0` in `examples/snippets/Cargo.toml`
- [x] T023 [P] [US4] Update `vim_rs` path dependency version to `0.6.0` in `examples/vtui/Cargo.toml`
- [x] T024 [US4] Retitle `CHANGELOG.md` section to `[0.6.0]`; add breaking-minor classification and spec-driven breaks including `EventFilterSpec` (FR-011 partial)
- [x] T037 [P] [US4] Update `vim_rs` version mention to **0.6.0** in `mcp/README.md`
- [x] T038 [US4] Run semver audit per `specs/003-vsphere-910-update/quickstart.md` Step 7 — confirm zero `0.5.1` release-target hits (SC-011)
- [ ] T058 [US4] Add **major dependency old→new lines** (`phf`, `phf_codegen`, `quick-xml`, `criterion`, `convert_case`, `check_keyword`, `bincode`, `tera`) and **one minor/patch summary line** to `CHANGELOG.md` `[0.6.0]` after US5 completes (FR-011, SC-006)

**Checkpoint**: Versions at 0.6.0; CHANGELOG documents API breaks and dependency majors.

---

## Phase 6: User Story 2 — Entire monorepo compiles cleanly (Priority: P1)

**Goal**: Every crate and sample project builds without errors after spec upgrade, version bump, and dependency refresh.

**Independent Test**: Full matrix in `specs/003-vsphere-910-update/quickstart.md` Step 3 passes with zero errors (SC-001, SC-002, SC-004).

**Depends on**: Phases 3, 5, 8 (bindings, versions, dependency refresh). **Re-run T059 after US5.**

### Implementation for User Story 2

- [x] T025 [US2] Run `cargo build` and `cargo test` in `vim_rs/` (default features)
- [x] T026 [US2] Run `cargo build --all-features` and `cargo test --all-features` in `vim_rs/`
- [x] T027 [P] [US2] Run `cargo test` in `vim_build/` and `openapi30/`
- [x] T028 [US2] Run `cargo build --bins` in `examples/` (all sample binaries, default features)
- [x] T029 [US2] Run `cargo build --bins --all-features` in `examples/`
- [x] T030 [US2] Fix compile errors in hand-written sample or test sources only for spec-driven API renames or removals — **do not** patch generated files (FR-010)
- [x] T031 [US2] Document breaking API symbols in `CHANGELOG.md` `[0.6.0]` (include `EventFilterSpec.auditable`, `EventFilterSpec.audit_id`)
- [x] T039 [US2] Re-run `cargo test -p vim_rs` and `cargo build --bins` in `examples/` after 0.6.0 semver correction
- [ ] T059 [US2] Re-run full compile/test matrix from `specs/003-vsphere-910-update/quickstart.md` Step 3 after US5 dependency refresh (includes `tls_rustls_only/` build)

**Checkpoint**: Full monorepo compile and test matrix green at **0.6.0** with refreshed dependencies.

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Release gates, metrics, audits, and final validation.

- [x] T032 Capture `vim_rs` debug and release build times and `examples/vtui` release binary size; compare against 0.5.0 baseline and note in `CHANGELOG.md` (SC-008)
- [x] T033 [P] Update `mcp/data_processing/data_transformer/README.md` if it still references the 9.0.x OpenAPI filename
- [x] T035 If any failure occurred during implementation, document root-cause classification per FR-013/SC-009 in `CHANGELOG.md` or PR notes
- [x] T036 Audit and update stale 9.0.x binding-target references per FR-014 (`README.md`, `CLAUDE.md`, `vim_rs/src/core/client.rs`, `mcp/README.md`)
- [x] T060 Confirm `specs/003-vsphere-910-update/quickstart.md` documents gitignored `mcp/data/` and local MCP regen workflow (FR-021)
- [ ] T061 Run full validation checklist from `specs/003-vsphere-910-update/quickstart.md` (all steps including Step 8 dependency audit) after US5 completes

---

## Phase 8: User Story 5 — Monorepo dependencies current and aligned (Priority: P2)

**Goal**: Full monorepo third-party dependency refresh with `phf` / `phf_codegen` **0.14** alignment and all major migrations (FR-016–FR-020).

**Independent Test**: `cargo outdated --root-deps-only` reports zero outdated direct deps per in-scope crate (SC-012); `vim_rs/Cargo.lock` has single `phf` 0.14.x major (SC-013); PHF regen idempotent (SC-014).

**Depends on**: Phase 2 complete. **PHF bumps (T040–T045) before other majors if regenerating PHF maps.**

### PHF 0.14 alignment (FR-017)

- [ ] T040 [P] [US5] Bump `phf_codegen` to `"0.14"` in `vim_build/Cargo.toml`
- [ ] T041 [P] [US5] Bump `phf` to `"0.14"` in `vim_macros/Cargo.toml`
- [ ] T042 [P] [US5] Bump `phf` to `"0.14"` in `vim_rs/Cargo.toml`
- [ ] T043 [P] [US5] Bump `phf` to `"0.14"` in `mcp/server/Cargo.toml`
- [ ] T044 [US5] Run `cargo update -p phf -p phf_codegen` in `vim_build/`, `vim_macros/`, `vim_rs/`, and `mcp/`; refresh respective `Cargo.lock` files
- [ ] T045 [US5] Re-run `cargo run --bin generate --release` from `vim_build/`; confirm idempotent second run on `vim_rs/src/types/struct_enum.rs`, `vim_rs/src/types/deserialize.rs`, `vim_macros/src/field_data.rs`, `mcp/server/src/field_data.rs` (SC-014)

### Remaining major bumps + source migrations (FR-018)

- [ ] T046 [P] [US5] Bump `convert_case` to `"0.11"` and `check_keyword` to `"0.4"` in `vim_build/Cargo.toml`; fix compile errors in `vim_build/src/vim_model/types.rs`, `vim_build/src/rs_emitter/names.rs`, and related emitters if APIs changed (R14)
- [ ] T047 [P] [US5] Bump `quick-xml` to `"0.40"` in `vim_rs/Cargo.toml`; fix compile errors under `vim_rs/src/xml/` if needed (R15)
- [ ] T048 [P] [US5] Bump `criterion` to `"0.8"` in `vim_rs/Cargo.toml` `[dev-dependencies]`; fix `vim_rs/benches/miniserde_bench.rs` if harness API changed (R15)
- [ ] T049 [P] [US5] Bump `bincode` to `"3"` in `mcp/server/Cargo.toml` and `mcp/data_processing/data_transformer/Cargo.toml`
- [ ] T050 [US5] Migrate `bincode` serialize/deserialize in `mcp/data_processing/data_transformer/src/main.rs` and `mcp/server/src/model.rs` to bincode 3 API (R11)
- [ ] T051 [P] [US5] Bump `tera` to `"2"` in `mcp/server/Cargo.toml`; fix `mcp/server/src/web_ui/handlers.rs` for tera 2 API if needed; verify with `cargo build -p vim_mcp_server --features web-ui` (R13)

### Minor/patch refresh + lockfiles (FR-019, FR-020)

- [ ] T052 [P] [US5] Bump stale minor/patch direct deps per [research R16](./research.md#r16--dependency-refresh-matrix) in `vim_rs/Cargo.toml`, `vim_build/Cargo.toml`, `openapi30/Cargo.toml`, `examples/snippets/Cargo.toml`, `examples/vtui/Cargo.toml`, `tls_rustls_only/Cargo.toml`, and `mcp/server/Cargo.toml`
- [ ] T053 [US5] Run `cargo update` in each in-scope crate/workspace and commit updated `Cargo.lock` files: `vim_rs/Cargo.lock`, `vim_macros/Cargo.lock`, `vim_build/Cargo.lock`, `openapi30/Cargo.lock`, `examples/Cargo.lock`, `mcp/Cargo.lock`, `tls_rustls_only/Cargo.lock` (FR-020)
- [ ] T054 [US5] Verify `vim_rs/Cargo.lock` contains only `phf` **0.14.x** entries — no 0.11.x or 0.13.x (SC-013): `rg '^name = "phf"' -A1 vim_rs/Cargo.lock`
- [ ] T055 [US5] Run `cargo outdated --root-deps-only` in `vim_rs/`, `vim_macros/`, `vim_build/`, `openapi30/`, `mcp/server/`, `examples/snippets/`, `examples/vtui/`, `tls_rustls_only/`; document any exceptions in `specs/003-vsphere-910-update/research.md` (SC-012)

**Checkpoint**: All direct dependencies current; single `phf` major in lockfile; migrations compile.

---

## Dependencies & Execution Order

### Phase Dependencies

```text
Setup (1) → Foundational (2) → US1 (3) ─┬→ US5 (8) ─→ US3 re-validate (T056–T057)
                                         ├→ US4 (5) ─→ T058 after US5
                                         └→ US2 re-validate (T059) after US5
Polish (7) T061 after US5 + T059
```

### User Story Dependencies

| Story | Priority | Status | Remaining |
|-------|----------|--------|-----------|
| US1 | P1 | ✅ Complete | — |
| US2 | P1 | ⚠️ Re-validate | T059 after US5 |
| US3 | P2 | ⚠️ Re-validate | T056–T057 after bincode 3 |
| US4 | P2 | ⚠️ CHANGELOG deps | T058 after US5 |
| US5 | P2 | 🔲 **Active** | T040–T055 |

### Parallel Opportunities (US5)

```bash
# PHF manifest bumps (after T040–T043):
T040: vim_build/Cargo.toml
T041: vim_macros/Cargo.toml
T042: vim_rs/Cargo.toml
T043: mcp/server/Cargo.toml

# Major migrations (after T045 regen):
T046: vim_build/ (convert_case, check_keyword)
T047: vim_rs/ (quick-xml)
T048: vim_rs/ (criterion)
T049: mcp/ (bincode manifests)
T051: mcp/server/ (tera)

# Minor/patch (after majors compile):
T052: all in-scope Cargo.toml files
```

### Parallel Example: PHF alignment

```bash
Task T040: vim_build/Cargo.toml phf_codegen = "0.14"
Task T041: vim_macros/Cargo.toml phf = "0.14"
Task T042: vim_rs/Cargo.toml phf = "0.14"
Task T043: mcp/server/Cargo.toml phf = "0.14"
# Then T044: cargo update locks
# Then T045: regenerate bindings (required before SC-014)
```

---

## Implementation Strategy

### Completed (MVP + release)

1. ✅ Setup + Foundational — spec paths wired  
2. ✅ US1 — 9.1.0.0 bindings generated (at pre-0.14 PHF)  
3. ✅ US3 — initial MCP local rebuild  
4. ✅ US4 — versions at 0.6.0  
5. ✅ US2 — initial compile matrix green  
6. ✅ Polish — metrics, stale-ref audit  

### Remaining (dependency refresh)

1. **US5 Phase 8** — PHF 0.14 → regen → major migrations → minor/patch → lockfiles  
2. **US3** — re-run local MCP pipeline after `bincode` 3 (T056–T057)  
3. **US2** — full matrix re-validation (T059)  
4. **US4** — finalize CHANGELOG dependency section (T058)  
5. **Polish** — full quickstart checklist (T061)  

### Failure Triage (FR-013)

On dependency or migration failure:

1. **Spec first** — only if generator fails (unlikely during dep-only work)  
2. **PHF order** — ensure `phf_codegen` 0.14 before regeneration (T045)  
3. **Migration APIs** — consult bincode 3 / tera 2 / convert_case 0.11 changelogs  
4. **Never** hand-edit generated PHF maps in `vim_rs/src/types/` or `field_data.rs` files  

---

## Notes

- **Total tasks**: **61** (T001–T061); **22 remaining** (T040–T045, T046–T055, T056–T059, T058, T061)
- **Completed**: T001–T039, T032–T036, T060 (39 tasks)
- `mcp/data/api_database.bin` is **gitignored** — local only (R12, FR-021)
- Generated files MUST NOT be hand-edited (Principle I)
- Suggested next step: **`/speckit-implement`** or execute Phase 8 (US5) tasks T040–T055
